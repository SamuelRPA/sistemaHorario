# API de producción en Render (npm run dev en localhost usa esta base).
# Si trabajas solo contra backend local, comenta la línea o pon http://127.0.0.1:4000
# y usa el proxy de Vite (rutas /api sin origen).
VITE_API_ORIGIN=https://sistemahorario.onrender.com
