# Opcional: origen absoluto del API (desarrollo con backend remoto o front estático aparte).
# Vacío en build de producción en el mismo dominio que el API = rutas relativas /api
# VITE_API_ORIGIN=https://sistemahorario.onrender.com

# Proxy de Vite en desarrollo (solo si NO usas VITE_API_ORIGIN)
# VITE_API_PROXY=http://127.0.0.1:4000
